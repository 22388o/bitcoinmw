	${IMPL_COMMENTS}
        ${VISIBILITY_IMPL} fn build_${VIEW_SNAKE_CASE} ${GENERIC_PRE} (options: Vec<${NAME}ConstOptions>) -> Result<impl ${TRAIT_LIST}, Error> ${WHERE_CLAUSE} {
		let conf = configure!(${NAME}Const, ${NAME}ConstOptions, options)?;
                ${NAME}::builder(conf)
        }
       
	${BOX_COMMENTS} 
        ${VISIBILITY_BOX} fn build_${VIEW_SNAKE_CASE}_box ${GENERIC_PRE} (options: Vec<${NAME}ConstOptions>) -> Result<Box<dyn ${TRAIT_LIST}>, Error> ${WHERE_CLAUSE} {
		let conf = configure!(${NAME}Const, ${NAME}ConstOptions, options)?;
                Ok(Box::new(${NAME}::builder(conf)?))
        } 
