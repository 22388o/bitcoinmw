steps:
  - script: |
      ./.ci/long_running_test.sh $(Build.Reason)
    displayName: Long running test
    condition: and(succeeded(), eq( variables['Agent.OS'], 'Linux' ))
  - script: |
      ./.ci/long_running_test.sh $(Build.Reason)
    displayName: Long running test
    condition: and(succeeded(), eq( variables['Agent.OS'], 'Darwin' ))
